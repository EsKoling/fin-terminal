---
description: Scaffold a new workspace panel
argument-hint: <panel-name>
---

Use the `panel-builder` agent to scaffold the `$1` panel.

Then register it in `apps/web/src/workspace/registry.ts` and add it to whichever preset
workspaces it belongs in. Run `npm run verify` before reporting done.
